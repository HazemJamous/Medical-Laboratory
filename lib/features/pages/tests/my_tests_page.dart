import 'package:flutter/material.dart';

class MyTestsPage extends StatelessWidget {
  const MyTestsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("MyTestsPage"));
  }
}